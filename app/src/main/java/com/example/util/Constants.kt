package com.example.util

object Constants {
    // If testing on local Android Emulator against local Node server, use: "http://10.0.2.2:3000/"
    // But since the backend is deployed to Render, we can use the live URL for 24/7 access!
    const val BASE_URL = "https://sharma-khata-bot.onrender.com/"
}
